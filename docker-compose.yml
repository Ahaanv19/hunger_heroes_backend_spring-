version: '3'
services:
  web:
    image: java_springv1
    build: .
    ports:
      - "8286:8286"
    volumes:
       - ./volumes:/app/volumes
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
